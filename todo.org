* DONE refactor org struct methods with self implementation in [[file: src/org/orgs.rs]]
* DONE Add location details to the Organization
* DONE Add customer details and dispatcher system
* DONE write rest api for the services defined in the logistics system
* DONE create swagger ui for the api routes defined
* DONE write unit tests for all the routes defined in the server module
* DONE Start planning how web interface for logistics system should look like, consider different UI options available and which UI tech stack to use
* DONE Check if all routes has unit tests coverted, if not write unit tests for those
* DONE Write integration tests for the UI dashboard
* DONE Swagger UI doc uploaded by the github action to the github pages doesn't connect to backend server yet, need to fix that - Fix - It connects to localhost backend
* DONE Need to plan for MySql test database flushing
  Plan written: [[file:docs/testing-database.md]]
  Phased approach, all three phases done:
** DONE Phase 1: test_support module (migrate + reset_database), dedicated logistics_test DB, serialize DB tests with serial_test
   Also fixed src/main.rs re-declaring mod logistics (caused cargo test to run twice, defeating #[serial])
** DONE Phase 2: DbConfig::from_env() - honour DATABASE_URL / MYSQL_* instead of hardcoded connection args. All 47 hardcoded `DbConnection::new(...)` call sites now use `DbConnection::from_env()`; verified against a live MySQL with both DATABASE_URL and MYSQL_* pointed at scratch databases.
** DONE Phase 3: database-per-test (logistics_test_<uuid> + Drop guard via TestDb), pool registry keyed by db name replacing the single global OnceLock<Pool>, #[serial(db)]/reset_database() dropped in favor of TestDb::create() on ~90 tests, serial_test dependency removed. Also fixed a connection-exhaustion bug this surfaced (many per-test pools at MySQL's default min:10/max:100 blew past max_connections=151 under nproc-wide parallelism, poisoning the pool-registry mutex and aborting the process via a double panic) by capping test pools to (1, 4) connections and recovering from a poisoned lock instead of propagating it. Verified: cargo test --all, 106 tests, 0 failures, ~11-14s (was ~50-55s serialized), 3 consecutive clean runs with no leftover databases.
* DONE Organisation can have multiple godowns where they store their inventory. Implemented in backend (Godown/Stock model, REST routes) and in the UI (Godowns section on org detail: create godown, add stock), with Rust route unit tests and Playwright UI tests covering the flow.
* DONE Rust unit tests on the github action should run once in 2 HR instead of 30 mins.
* Ground-ops research: how a freight/warehousing business actually runs a dispatch, and where this system currently diverges from it
  Godowns landed the storage layer; the gaps below are about the trip itself - the sequence of
  events a shipment goes through between "customer asked for stock" and "customer signed for it" -
  and the operational data (drivers, vehicle paperwork, billing) that a real dispatch depends on but
  this system does not yet record. Kept as a checklist, roughly in the order they'd get built.
** DONE Model dispatch as a lifecycle (PENDING -> CONFIRMED -> LOADED -> IN_TRANSIT -> DELIVERED / RETURNED / CANCELLED) with a timestamped status history, instead of writing a single "DISPATCHED" status once at creation and never updating it
   Plan written: [[file:docs/dispatch-lifecycle.md]]
   DispatchStatus enum + state machine, DispatchStatusHistory table, DispatchOrder::transition_to, and PUT /api/dispatches/{id}/status. dispatch_stock_to_customer now starts a dispatch at PENDING instead of the old fixed "DISPATCHED" string. 15 new tests (9 in dispatch.rs, 6 route-level). UI still treats status as an opaque string - not updated here.
** DONE Require proof of delivery (receiver name, signature or photo, delivery timestamp) before a dispatch can be marked DELIVERED
   Plan written: [[file:docs/dispatch-lifecycle.md]]
   DispatchOrder::transition_to now rejects a DELIVERED move without a ProofOfDeliveryInput (receiver_name + signature_or_photo_url); RETURNED has no such gate. Stored insert-only in a new DispatchProofOfDelivery table (dispatch_id PK), delivered_at stamped server-side same as the status-history entry, not caller-supplied. Exposed via PUT /api/dispatches/{id}/status's optional proof_of_delivery payload field and DispatchOrder.proof_of_delivery on responses. 8 new tests (4 in dispatch.rs, 4 route-level covering the missing-proof 400, the happy path, and that an illegal jump is still rejected even with proof attached).
** DONE [#B] Add a Driver entity (name, license number, phone) and require an active driver to be assigned to a vehicle before that vehicle can be selected for dispatch
   Driver model (id, org_id, name, license_number, phone, is_active) with
   CRUD routes (GET /api/drivers, POST /api/orgs/{id}/drivers, PUT/DELETE
   /api/drivers/{id}). Vehicle gains assigned_driver_id, set via
   PUT /api/vehicles/{reg}/driver (rejects a driver from another org).
   dispatch_stock_to_customer now only considers vehicles JOINed to an
   is_active driver; with none it returns a 400 telling the caller to
   assign one. Deleting a driver clears the assignment. Dashboard: a Drivers
   section on the org detail page (add / delete / toggle active) plus a
   driver dropdown per vehicle row in the fleet table.
** DONE [#B] Check the requested stock's volume/quantity against the selected vehicle's capacity before dispatch, and mark a vehicle unavailable while it has an active trip so `dispatch_stock_to_customer` can't double-book the same truck onto two orders at once
   Vehicle selection in dispatch_stock_to_customer now also filters on
   v.capacity >= (stock.volume_in_size * requested_quantity) and excludes
   any vehicle with a Dispatches row in a non-terminal status. A tiered
   error distinguishes "no vehicles" / "none with an active driver" /
   "none free and large enough". Freeing a trip (DELIVERED/RETURNED/
   CANCELLED) returns its vehicle to the pool.
** DONE Allow one dispatch to carry multiple stock line items - today `dispatch_stock_to_customer` takes a single `stock_description`, but a real shipment usually mixes several SKUs in one trip
   DispatchOrder now holds line_items: Vec<DispatchLineItem> { stock_description,
   quantity, volume_in_size } in a new DispatchLineItems table (cascade-deleted
   with the dispatch); the old Dispatches.stock_description / .quantity columns
   are gone, and drop_legacy_single_line_dispatch_tables recreates the dispatch
   tables when the old schema is detected. dispatch_stock_to_customer takes
   &[DispatchLineItemInput] and is all-or-nothing: rejects an empty list, a
   non-positive quantity, a repeated description, or any line that can't be met
   from the org's godowns, before drawing anything down; vehicle capacity is
   checked against the summed volume. POST /api/orgs/{id}/dispatch now takes
   { customer_id, line_items: [{ stock_description, requested_quantity }] }.
   Frontend: the dispatch form has repeatable stock-line rows (Add another line
   / remove), and the Dispatches + Dashboard tables list every line with a
   combined quantity. AI summary lists all line items. 6 new Rust tests
   (3 model, 3 route), 3 new frontend unit tests, 1 new Playwright flow.
   Plan/context: [[file:docs/dispatch-lifecycle.md]]
** DONE Support stock transfer between two godowns of the same org (distinct from dispatch-to-customer), with an audit trail of the move
   New StockTransfer model (src/logistics/godown/transfer.rs) + insert-only
   StockTransfers audit table. POST /api/godowns/{gid}/transfer takes
   {to_godown_id, description, quantity}: validates same-org / different-godown /
   sufficient source quantity / destination max_capacity, then in one MySQL
   transaction draws down the source, tops up the destination, and records the
   move. GET /api/orgs/{id}/stock-transfers returns the history newest-first.
   Frontend: a per-godown "Transfer" form (item + destination + quantity) plus a
   "Stock Transfers" history table on the org detail page. 12 new Rust tests
   (7 model, 5 route), 2 new godowns api-client tests, 2 new OrganizationDetail
   tests, 1 new Playwright flow. docs/godowns.md updated.
   Plan/context: [[file:docs/godowns.md]]
** DONE Add a low-stock / reorder threshold per stock item so a godown can be flagged when quantity drops below it
   Stock.reorder_threshold (Option<i64>) plus a server-computed below_threshold
   bool exposed on every read. Settable via POST/PUT /api/godowns/{gid}/stock.
** DONE Add a `max_capacity` to Godown and reject or warn on stock additions that would push it over
   Godown.max_capacity (Option<i64>) — a cap on Σ(volume_in_size × quantity)
   across the godown's stock. add/update stock over the cap is rejected 409.
   Set on create or PUT /api/godowns/{id}. None = unlimited.
** DONE Track vehicle compliance paperwork (insurance, RC, permit, PUC/fitness certificate) with expiry dates and renewal reminders - mandatory for any commercial truck on Indian roads
   New VehicleDocument model (src/logistics/vehicle/document.rs) + VehicleDocuments
   table, cascade-deleted with the vehicle and the org. doc_type is one of
   Insurance / RegistrationCertificate / Permit / PollutionCertificate /
   FitnessCertificate (RC/PUC/FC shorthands accepted). Dates are ISO strings,
   validated as real calendar dates; days_until_expiry and a Valid/ExpiringSoon
   (<= 30 days) / Expired status are computed on every read with a dependency-free
   days_from_civil helper. Routes: GET/POST /api/vehicles/{reg}/documents,
   PUT/DELETE /api/vehicle-documents/{id} (renew = PUT), and
   GET /api/orgs/{id}/vehicle-documents for the whole fleet. Frontend: a
   "Vehicle Compliance" section on the org detail page with expiring/expired
   count badges (the renewal reminder), a status-coded table, inline add form,
   and per-row Renew/delete. 14 new Rust tests (9 model, 5 route), 5 new vehicles
   api-client tests, 2 new OrganizationDetail tests, 1 new Playwright flow.
   Plan/context: [[file:docs/vehicle-compliance.md]]
** DONE Add freight billing: a cost per dispatch, invoice generation, and a payment status (paid/pending/overdue) per customer
   New Invoice model (src/logistics/billing/invoice.rs) + Invoices table, one
   invoice per dispatch (dispatch_id UNIQUE, cascade-deleted with the dispatch
   and the org). amount is a whole currency number (> 0); issued_on is stamped
   server-side, due_on is caller-supplied, and a PENDING / PAID / OVERDUE status
   is computed on every read from paid_on / due_on / today (ISO date strings,
   compared lexicographically). Routes: POST/GET /api/dispatches/{id}/invoice,
   PUT /api/invoices/{id} (amend while unpaid, 409 once paid),
   POST /api/invoices/{id}/pay (idempotent), GET /api/orgs/{id}/invoices, and
   GET /api/customers/{id}/billing for a customer's outstanding total +
   overdue count. Frontend: a Billing column on the Dispatches page (inline
   "Invoice" form + "Mark paid"), and an outstanding-balance / overdue column
   on the Customers page. 12 new Rust tests (9 model, 3 route), 9 new frontend
   unit tests, 1 new Playwright flow.
   Plan/context: [[file:docs/billing.md]]
** DONE Support returns: a customer return should credit stock back into a godown and record a RETURNED dispatch, rather than the system only ever being able to decrement stock
   Moving a dispatch IN_TRANSIT -> RETURNED now credits every line item's
   quantity back into a godown (bump an existing Stock row, or insert one with
   the line item's snapshotted volume_in_size). DispatchOrder::transition_to
   gained a return_to_godown_id: Option<Uuid> arg; when None,
   resolve_return_godown picks a godown already stocking one of the returned
   items, else the org's first godown by name (a godown from another org is
   rejected and the status is left unchanged). PUT /api/dispatches/{id}/status
   carries an optional return_to_godown_id on UpdateDispatchStatusPayload.
   Frontend: the Dispatches page's "Mark Returned" action opens an inline
   godown picker (default "auto"). 5 new Rust tests (3 model, 1 route, plus the
   existing lifecycle tests updated for the new arg), 3 new frontend unit tests,
   1 new Playwright flow.
   Plan/context: [[file:docs/dispatch-lifecycle.md]]
** DONE Move from one login per org to role-scoped user accounts within an org (admin, dispatcher, warehouse staff) so more than one person at the same org can log in with different permissions
   Additive: the org password stays and is the Admin. New src/logistics/user
   module — OrgUser (email-unique, bcrypt, is_active) with an OrgRole enum
   (ADMIN / DISPATCHER / WAREHOUSE_STAFF; from_str falls back to least
   privilege). POST /api/auth/user-login issues a role-scoped JWT (Claims
   gained user_id + role, serde-defaulted so old tokens still decode as
   Admin). AuthenticatedOrg carries the role; require_role() 403s gate user
   management + DELETE org (Admin), dispatch/dispatch-status/invoice
   create·amend·pay (Admin|Dispatcher), and godown + stock writes
   (Admin|Warehouse). Every role still reads everything. Admin-only user CRUD:
   GET/POST /api/orgs/{id}/users, PUT/DELETE /api/users/{id}. Frontend: a
   Login "Organization / Team member" tab switch (email login), a Team page
   (/team, Admin-only sidebar link) to add/edit/deactivate/remove members
   with inline role + status controls. 12 new Rust tests (7 model, 5 route),
   14 new frontend unit tests, 3 new Playwright flows, a demo-flow step, and a
   standalone `npm run test:e2e:demo:roles`. pages.yml + README updated.
   Follow-ups noted in the doc: no self-serve invite/reset, and only the ~8
   highest-impact write routes are gated.
   Plan/context: [[file:docs/roles.md]]
** DONE Add an endpoint for GPS trackers to push vehicle location automatically, instead of only the manual `PUT .../location` call
   Every vehicle now carries a random `tracker_key` (UUID, generated at
   registration, on every Vehicle response). A device POSTs
   `{latitude, longitude}` to `POST /api/track/{tracker_key}` with no login —
   the key is the whole credential (the only unauthenticated write in the
   API). Coordinates are range-checked (400), unknown key is 404, the time is
   server-stamped. `POST /api/vehicles/{reg}/tracker-key/rotate` (org bearer,
   owned) reissues the key if a device is lost. Schema: Vehicle.tracker_key,
   added to fresh DBs in CREATE TABLE and to old ones by a probe + ALTER +
   UUID() backfill helper. Frontend: a "GPS Tracker" panel on the vehicle
   detail page showing the push URL with a Regenerate button. 9 new Rust tests
   (4 model, 5 route), 5 new frontend unit tests, 2 new Playwright flows plus a
   demo-flow step. pages.yml expected-paths and README updated.
   Plan/context: [[file:docs/gps-tracking.md]]
** DONE Support multi-stop routes so one vehicle's trip can serve several customer orders in a sequence, instead of one dispatch always meaning one vehicle to one customer
   New Trip concept (src/logistics/dispatch/trip.rs) + Trips table; Dispatches
   gained trip_id + stop_sequence. A trip is a thin grouping — each stop is a
   full DispatchOrder with its own lifecycle / invoice / POD — and its status
   (PLANNED/IN_PROGRESS/COMPLETED) is derived from the stops. New
   Organization::dispatch_trip_to_customers: the single-dispatch flow was
   refactored into shared helpers (stock_snapshot/plan_stock_draw against a
   decrementing snapshot so two stops can't over-commit stock,
   select_free_vehicle, draw_down_plans); a trip needs >= 2 distinct located
   customers, fits the combined load onto one free vehicle (nearest to stop 1),
   is all-or-nothing, then writes the Trips row + one PENDING Dispatches row
   per stop and fires the create-notification for each. Routes (Admin|
   Dispatcher): POST/GET /api/orgs/{id}/trips, GET /api/trips/{id}. Frontend: a
   Trips page (/trips, sidebar link) with a repeatable-stop planning form and a
   card per trip; the Dispatches page tags trip-stop rows. Also fixed a latent
   bug in the notifications feature — `Notifications.seq` was only in CREATE
   TABLE, so a pre-existing local DB 500'd on the notifications list; added a
   probe + ALTER. 12 new Rust tests (4 trip model, 2 trip route, 6 refactor
   coverage retained), 7 frontend unit, 1 Playwright flow, a full-flow demo
   step, and `npm run test:e2e:demo:trips`. pages.yml + README updated.
   Plan/context: [[file:docs/multi-stop-routes.md]]
** DONE Add more units of measure (Kg, Litre, Box, Pallet, Piece) - `Unit` only has `MetricTon` today, which doesn't fit all cargo types
   Unit enum gains Kg/Litre/Box/Pallet/Piece; Unit::from_str is now
   case-insensitive with aliases and still falls back to MetricTon.
** DONE Send notifications (SMS/email) to the customer and driver when a dispatch is created and when it's delivered
   New src/logistics/notification module + Notifications table. On dispatch
   create → a notification for the customer and the assigned driver; on
   IN_TRANSIT→DELIVERED → one for the customer. Each row lands QUEUED (a
   message ready for a provider to send) or SKIPPED (no contact on file);
   nothing actually talks to Twilio/SMTP — that's a deploy-time integration,
   noted in the doc. Customer gained optional phone + email (settable on
   POST /api/orgs/{id}/customers, or Customer::set_contact); channel is email
   if set else SMS. Reads: GET /api/dispatches/{id}/notifications and
   GET /api/orgs/{id}/notifications (any org member). Frontend: Phone/Email
   fields on the customer form, and a 🔔 Notifications column on the Dispatches
   page that expands the recorded log per order. 11 new Rust tests (6 model,
   4 route, 1 customer-contact), 4 frontend unit, 1 Playwright flow, a
   full-flow demo step, and `npm run test:e2e:demo:notifications`. pages.yml +
   README updated.
   Plan/context: [[file:docs/notifications.md]]
** DONE Build basic ops reporting: vehicle utilization %, on-time delivery rate, stock turnover per godown, dispatch volume over time
   New src/logistics/reports module + GET /api/orgs/{id}/reports (bearer, own
   org only). All derived, no new tables — OpsReport::for_org folds the org's
   dispatch / vehicle / godown lists into: fleet utilization (owned vehicles on
   a non-terminal trip / total), delivery performance (delivered & returned
   counts, mean hours to DELIVERED, on-time rate against a 72h target since
   there is no promised-date field yet), per-godown inventory (units on hand,
   distinct items, Σvolume vs max_capacity), units dispatched in the last 30
   days, and a 14-day daily dispatch-volume series (zero-filled). Frontend: a
   Reports page (/reports, sidebar link) with four stat tiles, a dependency-free
   CSS bar chart, and a godown table. 8 new Rust tests (6 model, 2 route), 6 new
   frontend unit tests, 2 new Playwright flows. pages.yml expected-paths + README
   updated.
   Plan/context: [[file:docs/reports.md]]
* DONE Fix failing swagger ui doc generation github action - godown feature merge (PR #6) removed /api/orgs/{id}/stock* in favor of /api/orgs/{id}/godowns and /api/godowns/{gid}/..., but the workflow's expected-path list still checked for the old stock routes
* DONE sync readme with updated apis
  The README "API overview" table listed every route without the `/api` prefix
  the server mounts them under (`web::scope("/api")`). Reconciled the table
  against a freshly generated OpenAPI spec: prefixed every path with `/api`,
  added a note, and added rows for the new godown stock-transfer routes.
* DONE Customer should be linked to particular ORG and should be not shared
  Customers were a single flat, org-less table shared by every org. Now
  `Customer` carries a `NOT NULL org_id` foreign key (ON DELETE CASCADE from
  Orgs), the same way drivers and godowns are scoped. `GET /api/customers`
  returns only the authenticated org's customers; create moved to
  `POST /api/orgs/{id}/customers` (403 for another org); `PUT
  /api/customers/{id}/location` and a new `DELETE /api/customers/{id}` go
  through a `load_owned_customer` ownership check; and `POST
  /api/orgs/{id}/dispatch` rejects a customer_id belonging to another org.
  Frontend: the Customers page lists/creates under the logged-in org and
  gained a delete button. Legacy org-less `Customers` tables are dropped and
  recreated on migrate (same as the Stock/godown change). 8 new Rust tests,
  3 new frontend unit tests, 2 new Playwright flows.
  Plan/context: [[file:docs/customers.md]]
* DONE remove main branch, make master the default branch on GitHub - origin/HEAD still points at main, but every PR targets master
  `main` was stale — still sitting at the repo's very first commit while every
  PR has targeted `master` since. Set the GitHub repo's default branch to
  `master` (`gh repo edit --default-branch master`), then deleted `main` on
  origin (`gh api -X DELETE git/refs/heads/main`). Local `origin/HEAD` and
  remote-tracking refs updated to match (`git remote set-head origin -a`,
  `git fetch --prune`).
* DONE Add unit tests for all features implemented in the frontend
  Vitest + React Testing Library. Before this only dispatches (api client, lifecycle
  helpers, Dispatches page) had unit coverage. Added 16 new test files / 72 new tests
  (94 total): every api/ module (auth, client interceptors, orgs, customers, vehicles,
  godowns), every page (Login, Register, Organizations, OrganizationDetail, Dashboard,
  Customers, Vehicles), and the Sidebar/Navbar/LocationMap components. react-leaflet and
  LocationMap are mocked in page tests; router navigate/useParams are mocked per file.
  `npx tsc -b` and `npm run test:unit` both clean.
* DONE Add UI dashboard edit pages for vehicle, driver and godown details
  Clicking an entity's name in a list now opens a detail page with an
  editable, pre-filled form and a Save button: /vehicles/:reg (capacity,
  unit), /drivers/:id (name, licence, phone, active), /godowns/:id (name,
  address, max capacity). Backend gains the one missing update route,
  PUT /api/vehicles/{reg} {capacity, unit}, with a check_owned_vehicle
  403/404 ownership guard (Vehicle::org_of); drivers and godowns reuse
  their existing PUT routes, and updateGodown now always sends max_capacity
  so the cap can be cleared. 3 new Rust route tests, 9 new frontend unit
  tests, 4 new Playwright flows. Fleet displays now show the vehicle's real
  unit instead of a hardcoded "MT".
  Plan/context: [[file:docs/ui-editing.md]]
* DONE UI dashboard doesn't have an option to add stock to the org. Stock can only be added by API at the moment
  Already shipped: the "Organisation can have multiple godowns" work added a
  per-godown "Add Stock" inline form (Stock Item / Stock Quantity / Volume)
  on the org detail page's Godowns section, wired to
  POST /api/godowns/{gid}/stock via addGodownStock. Verified it's still there
  (src/pages/OrganizationDetail.tsx) and covered by
  frontend/tests/organization.spec.ts ("create a godown and add stock to it
  via the UI") and a step in e2e-full-flow.spec.ts. No further work needed;
  this entry was stale.
* TODO finish up manual steps to deploy frontend and backend on hosted platform
* DONE Add unit tests for rust backend
  Audited every src/logistics/* module for #[test]/#[actix_web::test]
  coverage: all domain modules already had it (262 tests, from the earlier
  "write unit tests for all routes" and "check if all routes have unit
  tests" items). The one real gap was src/logistics/ai/status.rs
  (generate_dispatch_summary), which had none. Pulled its prompt-building
  into a pure build_prompt(dispatch, customer) helper and added 5 tests: 4
  deterministic prompt-content cases (vehicle/stock/customer fields, the
  "location not set" and "none recorded" fallbacks, multiple line items
  joined) plus one for the missing-ANTHROPIC_API_KEY error path. The live
  Anthropic HTTP call itself is intentionally left untested — this codebase
  has no HTTP-mocking layer and hitting the real API in a test isn't
  appropriate. 251 Rust tests total, all passing.
* DONE Create release for the current state - Date [ 1-09-2026 ]
  v0.1.0 was tagged on this date but never actually released — release.yml's
  Docker build failed (fixed two days later by PR #20, never re-run) and the
  automated release-please flow that should have cut the next one has been
  failing on every push to master since, root-caused this session to two
  issues: it read its config from the repo's default branch, which was the
  stale `main` (see the "remove main branch" item above) - fixed by that
  change; then hit a second, separate blocker (GitHub Actions not permitted
  to open pull requests on this repo, a permissions setting) that's still
  open. Rather than leave "current state" unreleased indefinitely, cut
  v0.2.0 by hand: bumped Cargo.toml/.lock, frontend/package.json/-lock.json
  and .release-please-manifest.json 0.1.0 -> 0.2.0, wrote a CHANGELOG.md
  entry summarizing everything merged since v0.1.0, tagged and pushed
  v0.2.0. release.yml ran clean: linux/arm64 image on GHCR
  (ghcr.io/akashshindelogistics-lgtm/logistics-api:0.2.0), frontend tarball
  + openapi.json attached to the GitHub Release, deploy job correctly
  skipped (DEPLOY_ON_RELEASE unset - no hosting configured yet, see the
  "finish up manual deploy steps" item). Once the Actions PR-permission is
  fixed, release-please resumes normal automated operation from here.
  https://github.com/akashshindelogistics-lgtm/logistics/releases/tag/v0.2.0
* TODO Set ANTHROPIC_API_KEY on rust backend so that AI summery works
* DONE Customer create UI should have option to add location details of customer.
  The "Create Customer" form gained optional Latitude / Longitude number
  fields (address was already there). The page requires both or neither and
  range-checks them (-90..90 / -180..180) before submitting. `createCustomer`
  takes an optional location arg and folds latitude/longitude/location_address
  into the POST body. Backend: `CreateCustomerPayload` gained optional
  latitude/longitude/location_address; `add_customer` creates the customer and,
  when both coordinates are present, immediately calls `update_location` (a lone
  coordinate is ignored), so the response already carries the pin. A customer
  created with coordinates shows in the Location column and on the map right
  away. 2 new Rust route tests, 3 new frontend api-client tests, 2 new Customers
  page tests, 2 new Playwright flows. docs/customers.md + README updated.
  Plan/context: [[file:docs/customers.md]]
* TODO Update e2e demo flow as new changes + features are added to logistics system
* DONE All CI workflows should run on push to branch.
  The three test workflows (frontend-unit-tests, frontend-integration,
  periodic-tests) triggered only on push to main/master, so a feature branch
  got no CI until a PR was opened. Their push trigger is now branches: ['**']
  (any branch); periodic-tests keeps its 2-hourly schedule. pages.yml also
  widened to every branch, but its deploy job is gated to main/master with an
  `if` so a branch runs only the validate-OpenAPI + build-frontend checks and
  never republishes the live Pages site. Duplicate push+pull_request runs for
  same-repo branches are avoided by a per-job `if` that lets the pull_request
  event through only for forks, plus a per-(workflow, event, branch)
  concurrency group with cancel-in-progress. release-please.yml / release.yml /
  deploy-backend.yml are release/deploy automation, not CI, and are left on
  master / tag / manual triggers. All four edited workflows pass actionlint.
  Plan/context: [[file:docs/ci-on-branch-push.md]]
* AI opportunities in the logistics system - where generative AI can genuinely help run this business, and where it would just be a gimmick
  The one existing AI feature (generate_dispatch_summary, src/logistics/ai/status.rs)
  is prompt templating over already-fetched fields - no retrieval, no embeddings,
  despite a commit calling it "RAG". Most of this app's data is structured (dispatches,
  stock, invoices, reports), which Claude can narrate directly without retrieval;
  genuine retrieval only pays off over the actual free-text corpus that already
  exists - Notification.body, VehicleDocument.notes - plus narratives synthesized
  from structured status history. Kept as a checklist, roughly in priority order.
  No new AI vendor: everything below uses only ANTHROPIC_API_KEY, already the
  app's one AI dependency.
** TODO Org-scoped "ask your data" assistant - a natural-language Q&A widget grounded in retrieval over the org's own notifications, compliance notes and dispatch history. The one genuine RAG opportunity in this system.
   Retrieval via MySQL FULLTEXT search (no embeddings, no second AI vendor):
   a new AiChunks table (org_id, kind, source_id, text, updated_at) with a
   FULLTEXT INDEX on `text`, ensure_table'd and kept in sync with
   test_support::migrate like every other table. v1 chunk kinds, one row each:
     - notification: one chunk per Notification row (already free text).
     - vehicle_document: one chunk per VehicleDocument row (doc_type + notes).
     - dispatch_narrative: one chunk per DispatchOrder, REGENERATED WHOLE
       (not appended) on every status change - synthesizes customer + line
       items + full status_history + proof of delivery into one narrative
       paragraph, since "what happened with dispatch X" needs the structured
       history turned into text somewhere.
     - stock_snapshot: one chunk per Stock row (godown + description),
       regenerated on add/update/transfer - covers "what's low on stock"
       questions. source_id = "{godown_id}:{description}" (Stock has no own
       id, matching Vehicle's use of a non-UUID string key elsewhere).
   Explicitly out of v1: OpsReport-derived chunks (too volatile to keep
   indexed - see Phase 4 below instead), org_directory (customer/godown/
   vehicle address chunks - Phase 3), persisting AI-generated summaries as a
   new corpus source.
   Indexing is write-through and best-effort: whenever a route already
   writes one of the source rows, it also upserts (INSERT ... ON DUPLICATE
   KEY UPDATE, keyed on (org_id, kind, source_id)) the corresponding chunk -
   no background job/scheduler. No embedding call, so nothing to fail
   loudly on; this is just an extra INSERT alongside the write that already
   happens.
   Retrieval flow (src/logistics/ai/index.rs): given a question, run
   `SELECT *, MATCH(text) AGAINST (:q IN NATURAL LANGUAGE MODE) AS score
   FROM AiChunks WHERE org_id = :org_id HAVING score > 0 ORDER BY score DESC
   LIMIT 8` (org_id filter is the tenant-isolation boundary, same as every
   other list_by_org). If zero rows match, skip straight to a canned "no
   data yet" answer rather than calling Claude with empty context.
   Generation (src/logistics/ai/assistant.rs): reuse the exact Anthropic
   call shape from ai/status.rs (same model, same reqwest pattern, same
   x-api-key/anthropic-workspace-id headers) with a new pure
   build_answer_prompt(question, chunks) function - "answer only using the
   facts below, say so plainly if they don't contain the answer" - so it's
   unit-testable without a live call, same as build_prompt today.
   Route: POST /api/orgs/{id}/assistant/ask, AuthenticatedOrg-gated with the
   same org_id-path-param 403 check get_ops_report uses. Request
   {question}, response {answer, sources: [{kind, source_id, excerpt}]}.
   Frontend: a global floating widget (frontend/src/components/
   AssistantWidget.tsx), not a page or Sidebar link - the target questions
   ("what happened with dispatch X") come up while already looking at some
   other page. Bottom-right circular launcher reusing .btn-ai's indigo
   gradient and "✦" glyph; expands a fixed panel styled like
   .ai-summary-card with a scrollable Q&A history, a text input, and the
   existing .ai-summary-loading/.ai-pulse loading state. New API client
   frontend/src/api/assistant.ts following the flat-arrow-function
   convention every other api/*.ts file uses.
   Testing (matches this codebase's "don't hit live APIs in tests"
   convention): the per-kind chunk-text builders, build_answer_prompt, and
   (new, since there's no embedding step to test instead) nothing needs a
   cosine-similarity unit test now - MySQL does the ranking. Route tests:
   403/401/400 shapes like the existing dispatch-summary tests, plus a
   TestDb-backed org-isolation test on AiChunks (mirrors Customer::list_by_org's
   scoping test).
   Phased (one PR each, backend+frontend+tests together per this repo's
   convention):
*** DONE Phase 1: notification + vehicle_document chunks, the full retrieval/generation/route/widget vertical slice
    A complete, demoable feature even though only two chunk kinds are
    indexed. AiChunks table (src/logistics/ai/chunk.rs, FULLTEXT INDEX on
    text, kept in sync with test_support::migrate) with a ChunkKind enum
    (Notification | VehicleDocument), upsert/upsert_best_effort/
    delete_by_source_best_effort, and search_by_org (the MATCH...AGAINST
    query). Write-through hooks added to Notification::persist and
    VehicleDocument::create/update/delete. Generation in
    src/logistics/ai/assistant.rs (build_answer_prompt + answer_question),
    reusing ai/status.rs's exact Anthropic call shape; returns a canned
    "nothing indexed yet" answer without calling Claude when retrieval
    finds nothing. Route: POST /api/orgs/{id}/assistant/ask. Frontend: a
    global AssistantWidget (frontend/src/components/AssistantWidget.tsx),
    mounted once in AppShell outside the routed pages, reusing the .btn-ai/
    .ai-summary-card/.ai-summary-loading/.ai-pulse AI visual language; new
    frontend/src/api/assistant.ts. 16 new Rust unit tests (chunk-text
    builders, build_answer_prompt, retrieval org-isolation/ranking/upsert
    semantics, missing-API-key path) + 2 write-through integration tests
    (recording a notification / creating a vehicle document indexes it,
    deleting the document removes it) + 4 route tests, 2 new frontend
    api-client tests, 4 new AssistantWidget component tests, 3 new
    Playwright flows, a standalone `npm run test:e2e:demo:assistant`. No
    step added to e2e-full-flow.spec.ts, matching the existing AI
    dispatch-summary feature's precedent - the live-answer path needs
    ANTHROPIC_API_KEY, not configured in this environment.
*** DONE Phase 2: dispatch_narrative + stock_snapshot chunks and their write-through hooks (on DispatchOrder::transition_to and Stock update/transfer)
    No frontend changes needed - the widget already renders whatever
    sources[] come back generically. ChunkKind extended with
    DispatchNarrative | StockSnapshot; dispatch_narrative_text (customer +
    line items + full status_history + proof of delivery, regenerated
    whole on every status change, not appended) and stock_snapshot_text
    (godown + description + quantity + below_threshold flag) added to
    src/logistics/ai/chunk.rs alongside reindex_dispatch_best_effort and
    reindex_stock_by_description_best_effort. Write-through hooks wired
    into seven call sites: DispatchOrder::save/transition_to/
    credit_line_items_back (src/logistics/dispatch/dispatch.rs),
    Stock::add_to_godown/update_in_godown/remove_from_godown
    (src/logistics/stock/stock.rs), StockTransfer::execute
    (src/logistics/godown/transfer.rs), and Organization::draw_down_plans
    (src/logistics/orgs/orgs.rs, the shared stock-decrement path used by
    every dispatch). 4 new pure unit tests for the chunk-text builders +
    6 new integration tests proving the index tracks reality end to end:
    dispatch save/transition regenerates the narrative whole, a RETURNED
    transition credits stock back into the chunk, add/update/remove keep
    a stock chunk in sync (including deletion once quantity hits zero),
    a transfer reindexes both the source and destination godowns'
    chunks (and deletes the source chunk when fully drained), and
    dispatch_stock_to_customer's draw-down reflects in the stock chunk.
    Full `cargo test --lib` suite (284 tests) green with no regressions.
*** DONE Phase 3: org_directory chunks (customer/godown/vehicle address/identity) plus a reindex_org(org_id) backfill function
    For orgs whose existing rows predate this feature - nothing re-triggers
    their write-through hook until next touched. Added ChunkKind::OrgDirectory
    (source_id prefixed "customer:"/"godown:"/"vehicle:" so the three entity
    types can't collide under the shared kind) with customer_directory_text,
    godown_directory_text and vehicle_directory_text (the latter looks up the
    vehicle's currently assigned driver, if any, via Driver::get_by_id) in
    src/logistics/ai/chunk.rs. Write-through hooks wired into
    Customer::create_customer/set_contact/delete, Godown::create/update/
    remove, and Vehicle::add_new_vehicle_to_org/update_vehicle/assign_driver/
    remove_vehicle (update_location was deliberately left unhooked on all
    three - it only ever changes `location`, never the `address`/identity
    fields the directory text is built from, so reindexing there would be a
    silent no-op write). reindex_org(org_id) added as a full backfill,
    rebuilding every chunk kind (org directory + notifications + vehicle
    documents + dispatch narratives + stock snapshots) from scratch in one
    call - exposed via POST /api/orgs/{id}/assistant/reindex (AuthenticatedOrg-
    gated, same org_id-path-param 403 check as ask_assistant) and a "Reindex
    my data" link in the AssistantWidget panel footer. 8 new pure unit tests
    (3 directory-text builders x2 each, plus reindex_org's own backfill
    test) + 3 new write-through integration tests (Customer/Godown/Vehicle
    each keep their chunk in sync through create/update/delete) + 3 new
    route tests + 2 new frontend api-client/component tests + 1 new
    Playwright flow. Full `cargo test --lib` (297 tests), `npm run
    test:unit` (185 tests) and `npm run test:e2e` (62 tests) all green with
    no regressions.
*** TODO (optional) Phase 4: structured OpsReport grounding for aggregate-shaped questions ("what's my fleet utilization")
    Inject a small always-current OpsReport::for_org snapshot directly into
    the generation prompt alongside retrieved chunks, rather than trying to
    keep volatile numbers indexed as chunks that go stale every dispatch.
** TODO "Explain this report" - an AI-narrated summary on the Reports page, same shape as the existing AI dispatch summary but pointed at OpsReport instead of one dispatch
   Not RAG - straight prompt-over-structured-data, exactly like
   generate_dispatch_summary already does for one dispatch. New
   build_report_prompt(report: &OpsReport) -> String (pure, unit-testable)
   feeding fleet utilization, delivery performance, 14-day volume trend and
   per-godown inventory into Claude, asking it to call out what's actually
   worth a dispatcher's attention (e.g. "3 vehicles are idle, utilization
   is unusually low this week" or "on-time rate dropped from last period") -
   the kind of narrative-over-numbers judgment call is exactly what an LLM
   is good at and a static dashboard tile can't do. Same GET-on-demand route
   shape as /api/dispatches/{id}/summary; same .btn-ai/.ai-summary-card
   frontend treatment, placed above the stat-tile grid on Reports.tsx.
** TODO Daily ops digest - an on-demand (not scheduled) AI summary of "what needs attention today" across dispatches, compliance and stock
   Also prompt-over-structured-data, one level broader than the report
   narration above: pulls together dispatches stuck in a status too long
   (compare status_history's last-changed timestamp against now), vehicles
   with ExpiringSoon/Expired compliance documents, and godown stock rows
   with below_threshold set - all already-computed fields, no new queries
   of consequence - and asks Claude to prioritize them into a short
   actionable list. Natural home: a dashboard card, or fold into the
   assistant widget above as a canned starter question once that exists.
   Worth building this one AFTER the assistant widget (previous item) even
   though it doesn't need retrieval, since the widget gives it a place to
   live for free.
** TODO Smart reorder suggestions - narrate low-stock godowns with a suggested reorder quantity based on recent dispatch velocity, not just the current below_threshold flag
   Needs one new piece of structured computation this system doesn't have
   yet - per-item dispatch velocity (units of a given stock description
   dispatched per week, derived from existing DispatchLineItems +
   dispatched_at, no new table) - before AI narration is useful here; the
   AI part itself is again prompt-over-structured-data. Lower priority than
   the two items above since it requires that extra computation first and
   below_threshold already covers the "something needs attention" signal;
   this only adds "by how much."
** TODO (lower priority, blocked on missing infra) Proof-of-delivery photo verification via a vision-capable model
   ProofOfDelivery.signature_or_photo_url is free-form and, per
   docs/dispatch-lifecycle.md, has no real file-storage backing it today -
   callers pass a URL or inline data: URI with nothing validating it's
   actually a photo/signature. A vision-capable Claude call could flag
   "this doesn't look like a signature or package photo" before a dispatch
   is marked DELIVERED, but it's not worth building until proof-of-delivery
   has real file storage behind it - otherwise there's nothing reliable to
   verify.
** TODO (blocked on notification delivery) AI-personalized notification copy
   Notification.body is already free text (currently a fixed template), but
   per docs/notifications.md nothing in this repo actually sends a
   notification anywhere - no Twilio/SMTP is wired up. Personalizing the
   copy with AI before delivery exists to receive it is solving the wrong
   problem first; revisit once notifications are actually sent.
